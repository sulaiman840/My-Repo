import 'package:flutter/material.dart';

import 'widget/all_type_view_body_manager.dart';

class AllTypeViewManager extends StatelessWidget {
  const AllTypeViewManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AllTypeViewBodyManager();
  }
}
