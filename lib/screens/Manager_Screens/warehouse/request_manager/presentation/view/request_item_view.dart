import 'package:flutter/material.dart';

import 'widget/request_item_view_body.dart';

class RequestItemView extends StatelessWidget {
  const RequestItemView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return RequestItemViewBody();
  }
}
