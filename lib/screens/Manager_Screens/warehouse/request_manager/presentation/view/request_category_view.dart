import 'package:flutter/material.dart';

import 'widget/request_category_view_body.dart';

class RequestCategoryView extends StatelessWidget {
  const RequestCategoryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const RequestCategoryViewBody();
  }
}
